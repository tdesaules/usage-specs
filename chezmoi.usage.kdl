name "chezmoi"
bin "chezmoi"

    flag "-c" help="Set config file"
    flag "-D" help="Set destination directory"
    flag "-h" help="help for chezmoi"
    flag "-k" help="Keep going as far as possible after an error"
    flag "-n" help="Do not make any modifications to the destination directory"
    flag "-o" help="Write output to path instead of stdout"
    flag "-R" help="Refresh external cache"
    flag "-S" help="Set source directory"
    flag "-v" help="Make output more verbose"
    flag "-W" help="Set working tree directory"
    flag "--age-recipient" help="Override age recipient"
    flag "--age-recipient-file" help="Override age recipient"
    flag "--cache" help="Set cache directory"
    flag "--color" help="Colorize output"
    flag "--config" help="Set config file"
    flag "--config-format" help="Set config file format"
    flag "--debug" help="Include debug information in output"
    flag "--destination" help="Set destination directory"
    flag "--dry-run" help="Do not make any modifications to the destination directory"
    flag "--force" help="Make all changes without prompting"
    flag "--help" help="help for chezmoi"
    flag "--interactive" help="Prompt for all changes"
    flag "--keep-going" help="Keep going as far as possible after an error"
    flag "--less-interactive" help="Prompt for changed or pre-existing targets"
    flag "--mode" help="Mode"
    flag "--no-pager" help="Do not use the pager"
    flag "--no-tty" help="Do not attempt to get a TTY for prompts"
    flag "--output" help="Write output to path instead of stdout"
    flag "--override-data" help="Override data"
    flag "--override-data-file" help="Override data with file"
    flag "--persistent-state" help="Set persistent state file"
    flag "--progress" help="Display progress bars"
    flag "--refresh-externals" help="Refresh external cache"
    flag "--source" help="Set source directory"
    flag "--source-path" help="Specify targets by source path"
    flag "--use-builtin-age" help="Use builtin age"
    flag "--use-builtin-diff" help="Use builtin diff"
    flag "--use-builtin-git" help="Use builtin git"
    flag "--verbose" help="Make output more verbose"
    flag "--version" help="version for chezmoi"
    flag "--working-tree" help="Set working tree directory"

    cmd "add" help="Add an existing file, directory, or symlink to the source state" {
        flag "-a" help="Generate the template when adding files as templates"
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-f" help="Add symlink targets instead of symlinks"
        flag "-h" help="help for add"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-p" help="Prompt before adding each entry"
        flag "-q" help="Suppress warnings"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-T" help="Add files as templates"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--autotemplate" help="Generate the template when adding files as templates"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--create" help="Add files that should exist, irrespective of their contents"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--encrypt" help="Encrypt files"
        flag "--exact" help="Add directories exactly"
        flag "--exclude" help="Exclude entry types"
        flag "--follow" help="Add symlink targets instead of symlinks"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for add"
        flag "--include" help="Include entry types"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--new" help="Create new file if target does not exist"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--prompt" help="Prompt before adding each entry"
        flag "--quiet" help="Suppress warnings"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--secrets" help="Scan for secrets when adding unencrypted files"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--template" help="Add files as templates"
        flag "--template-symlinks" help="Add symlinks with target in source or home dirs as templates"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "age" help="Interact with age" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for age"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for age"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"

        cmd "decrypt" help="Decrypt file or standard input" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for decrypt"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-p" help="Decrypt with a passphrase"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for decrypt"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--passphrase" help="Decrypt with a passphrase"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "encrypt" help="Encrypt file or standard input" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for encrypt"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-p" help="Encrypt with a passphrase"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for encrypt"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--passphrase" help="Encrypt with a passphrase"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }
    }

    cmd "age-keygen" help="Generate an age identity or convert an age identity to an age recipient" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for age-keygen"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-y" help="convert identities to recipients"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--convert" help="convert identities to recipients"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for age-keygen"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--pq" help="generate a post-quantum key pair"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "apply" help="Update the destination directory to match the target state" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for apply"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-P" help="Apply all parent directories"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for apply"
        flag "--include" help="Include entry types"
        flag "--init" help="Recreate config file from template"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--parent-dirs" help="Apply all parent directories"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "archive" help="Generate a tar archive of the target state" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-f" help="Set archive format"
        flag "-h" help="help for archive"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-P" help="Archive parent directories"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "-z" help="Compress output with gzip"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--format" help="Set archive format"
        flag "--gzip" help="Compress output with gzip"
        flag "--help" help="help for archive"
        flag "--include" help="Include entry types"
        flag "--init" help="Recreate config file from template"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--parent-dirs" help="Archive parent directories"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "cat" help="Print the target contents of a file, script, or symlink" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for cat"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for cat"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "cat-config" help="Print the configuration file" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for cat-config"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for cat-config"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "cd" help="Launch a shell in the source directory" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for cd"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for cd"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "chattr" help="Change the attributes of a target in the source state" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for chattr"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for chattr"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"

        cmd "after" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "before" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "create" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "empty" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "encrypted" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "exact" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "executable" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "external" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "modify" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noafter" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "nobefore" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "nocreate" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noempty" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noencrypted" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noexact" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noexecutable" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noexternal" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "nomodify" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noonce" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noonchange" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noprivate" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noreadonly" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noremove" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "noscript" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "nosymlink" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "notemplate" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "once" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "onchange" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "private" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "readonly" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "remove" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "script" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "symlink" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "template" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for chattr"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-r" help="Recurse into subdirectories"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for chattr"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--recursive" help="Recurse into subdirectories"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }
    }

    cmd "completion" help="Generate shell completion code" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for completion"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for completion"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"

        cmd "bash" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for completion"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for completion"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "fish" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for completion"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for completion"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "powershell" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for completion"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for completion"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "zsh" help="" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for completion"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for completion"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }
    }

    cmd "data" help="Print the template data" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-f" help="Output format"
        flag "-h" help="help for data"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--format" help="Output format"
        flag "--help" help="help for data"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "decrypt" help="Decrypt file or standard input" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for decrypt"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for decrypt"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "destroy" help="Permanently delete an entry from the source state, the destination directory, and the state" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for destroy"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for destroy"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "diff" help="Print the diff between the target state and the destination state" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for diff"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-P" help="Print the diff of all parent directories"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for diff"
        flag "--include" help="Include entry types"
        flag "--init" help="Recreate config file from template"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--pager" help="Set pager"
        flag "--parent-dirs" help="Print the diff of all parent directories"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--reverse" help="Reverse the direction of the diff"
        flag "--script-contents" help="Show script contents"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "docker" help="Use your dotfiles in a Docker container" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for docker"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for docker"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"

        cmd "exec" help="Install your dotfiles in an existing Docker container and execute a shell" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for exec"
            flag "-i" help="Run interactively"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-p" help="Package manager"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-s" help="Execute shell afterwards"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for exec"
            flag "--interactive" help="Run interactively"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--package-manager" help="Package manager"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--shell" help="Execute shell afterwards"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "run" help="Create a new Docker container with your dotfiles and execute a shell" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for run"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-p" help="Package manager"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for run"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--package-manager" help="Package manager"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }
    }

    cmd "doctor" help="Check your system for potential problems" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for doctor"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for doctor"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-network" help="do not use network connection"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "dump" help="Generate a dump of the target state" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-f" help="Output format"
        flag "-h" help="help for dump"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-P" help="Dump all parent directories"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--format" help="Output format"
        flag "--help" help="help for dump"
        flag "--include" help="Include entry types"
        flag "--init" help="Recreate config file from template"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--parent-dirs" help="Dump all parent directories"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "dump-config" help="Dump the configuration values" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-f" help="Output format"
        flag "-h" help="help for dump-config"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--format" help="Output format"
        flag "--help" help="help for dump-config"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "edit" help="Edit the source state of a target" {
        flag "-a" help="Apply after editing"
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for edit"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--apply" help="Apply after editing"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--hardlink" help="Invoke editor with a hardlink to the source file"
        flag "--help" help="help for edit"
        flag "--include" help="Include entry types"
        flag "--init" help="Recreate config file from template"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--watch" help="Apply on save"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "edit-config" help="Edit the configuration file" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for edit-config"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for edit-config"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "edit-config-template" help="Edit the configuration file template" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for edit-config-template"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for edit-config-template"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "edit-encrypted" help="Edit an encrypted file" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for edit-encrypted"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for edit-encrypted"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "encrypt" help="Encrypt file or standard input" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for encrypt"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for encrypt"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "execute-template" help="Execute the given template(s)" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-f" help="Treat arguments as filenames"
        flag "-h" help="help for execute-template"
        flag "-i" help="Simulate chezmoi init"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-p" help="Simulate promptString"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--file" help="Treat arguments as filenames"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for execute-template"
        flag "--init" help="Simulate chezmoi init"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--left-delimiter" help="Set left template delimiter"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--promptBool" help="Simulate promptBool"
        flag "--promptChoice" help="Simulate promptChoice"
        flag "--promptInt" help="Simulate promptInt"
        flag "--promptMultichoice" help="Simulate promptMultichoice"
        flag "--promptString" help="Simulate promptString"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--right-delimiter" help="Set right template delimiter"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--stdinisatty" help="Simulate stdinIsATTY"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--with-stdin" help="Set .chezmoi.stdin to the contents of the standard input"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "forget" help="Remove a target from the source state" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for forget"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for forget"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "generate" help="Generate a file for use with chezmoi" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for generate"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for generate"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"

        cmd "git-commit-message" help="Generate a git commit message" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for git-commit-message"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for git-commit-message"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }
    }

    cmd "git" help="Run git in the source directory" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for git"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for git"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "help" help="Print help about a command" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for help"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for help"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "ignored" help="Print ignored targets" {
        flag "-0" help="Use the NUL character as a path separator"
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for ignored"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-t" help="Print paths as a tree"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for ignored"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--nul-path-separator" help="Use the NUL character as a path separator"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--tree" help="Print paths as a tree"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "import" help="Import an archive into the source state" {
        flag "-c" help="Set config file"
        flag "-d" help="Set destination prefix"
        flag "-h" help="help for import"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Remove destination before import"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination prefix"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exact" help="Set exact_ attribute on imported directories"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for import"
        flag "--include" help="Include entry types"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--remove-destination" help="Remove destination before import"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--strip-components" help="Strip leading path components"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "init" help="Setup the source directory and update the destination directory to match the target state" {
        flag "-a" help="Update destination directory"
        flag "-C" help="Path to write generated config file"
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-d" help="Create a shallow clone"
        flag "-g" help="Guess the repo URL"
        flag "-h" help="help for init"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-P" help="Purge chezmoi binary after running"
        flag "-p" help="Purge config and source directories after running"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--apply" help="Update destination directory"
        flag "--branch" help="Set initial branch to checkout"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--config-path" help="Path to write generated config file"
        flag "--data" help="Include existing template data"
        flag "--debug" help="Include debug information in output"
        flag "--depth" help="Create a shallow clone"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--git-lfs" help="Run git pull lfs after cloning"
        flag "--guess-repo-url" help="Guess the repo URL"
        flag "--help" help="help for init"
        flag "--include" help="Include entry types"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--one-shot" help="Run in one-shot mode"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--prompt" help="Force prompt*Once template functions to prompt"
        flag "--promptBool" help="Populate promptBool"
        flag "--promptChoice" help="Populate promptChoice"
        flag "--promptDefaults" help="Make prompt functions return default values"
        flag "--promptInt" help="Populate promptInt"
        flag "--promptMultichoice" help="Populate promptMultichoice"
        flag "--promptString" help="Populate promptString"
        flag "--purge" help="Purge config and source directories after running"
        flag "--purge-binary" help="Purge chezmoi binary after running"
        flag "--recurse-submodules" help="Checkout submodules recursively"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--ssh" help="Use ssh instead of https when guessing repo URL"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "license" help="Print license" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for license"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for license"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "managed" help="List the managed entries in the destination directory" {
        flag "-0" help="Use the NUL character as a path separator"
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-f" help="Format"
        flag "-h" help="help for managed"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-p" help="Path style"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-t" help="Print paths as a tree"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--format" help="Format"
        flag "--help" help="help for managed"
        flag "--include" help="Include entry types"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--nul-path-separator" help="Use the NUL character as a path separator"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--path-style" help="Path style"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--tree" help="Print paths as a tree"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "merge" help="Perform a three-way merge between the destination state, the source state, and the target state" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for merge"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for merge"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "merge-all" help="Perform a three-way merge for each modified file" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for merge-all"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for merge-all"
        flag "--init" help="Recreate config file from template"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "purge" help="Purge chezmoi's configuration and data" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for purge"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-P" help="Purge chezmoi binary"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--binary" help="Purge chezmoi binary"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for purge"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "re-add" help="Re-add modified files" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for re-add"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for re-add"
        flag "--include" help="Include entry types"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--re-encrypt" help="Re-encrypt encrypted files"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "secret" help="Interact with a secret manager" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for secret"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for secret"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"

        cmd "keyring" help="Interact with keyring" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for keyring"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for keyring"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"

            cmd "delete" help="Delete a value from keyring" {
                flag "--service" help="service"
                flag "--user" help="user"
            }

            cmd "get" help="Get a value from keyring" {
                flag "--service" help="service"
                flag "--user" help="user"
            }

            cmd "set" help="Set a value in keyring" {
                flag "--service" help="service"
                flag "--user" help="user"
            }
        }
    }

    cmd "source-path" help="Print the source path of a target" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for source-path"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for source-path"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "ssh" help="SSH to a host and initialize dotfiles" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for ssh"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-p" help="Package manager"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-s" help="Execute shell afterwards"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for ssh"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--package-manager" help="Package manager"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--shell" help="Execute shell afterwards"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "state" help="Manipulate the persistent state" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for state"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for state"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"

        cmd "data" help="Print the raw data in the persistent state" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-f" help="Output format"
            flag "-h" help="help for data"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--format" help="Output format"
            flag "--help" help="help for data"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "delete" help="Delete a value from the persistent state" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for delete"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--bucket" help="Bucket"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for delete"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--key" help="Key"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "delete-bucket" help="Delete a bucket from the persistent state" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for delete-bucket"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--bucket" help="Bucket"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for delete-bucket"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "dump" help="Generate a dump of the persistent state" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-f" help="Output format"
            flag "-h" help="help for dump"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--format" help="Output format"
            flag "--help" help="help for dump"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "get" help="Get a value from the persistent state" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for get"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--bucket" help="Bucket"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for get"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--key" help="Key"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "get-bucket" help="Get a bucket from the persistent state" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-f" help="Output format"
            flag "-h" help="help for get-bucket"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--bucket" help="bucket"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--format" help="Output format"
            flag "--help" help="help for get-bucket"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "reset" help="Reset the persistent state" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for reset"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for reset"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }

        cmd "set" help="Set a value from the persistent state" {
            flag "-c" help="Set config file"
            flag "-D" help="Set destination directory"
            flag "-h" help="help for set"
            flag "-k" help="Keep going as far as possible after an error"
            flag "-n" help="Do not make any modifications to the destination directory"
            flag "-o" help="Write output to path instead of stdout"
            flag "-R" help="Refresh external cache"
            flag "-S" help="Set source directory"
            flag "-v" help="Make output more verbose"
            flag "-W" help="Set working tree directory"
            flag "--age-recipient" help="Override age recipient"
            flag "--age-recipient-file" help="Override age recipient"
            flag "--bucket" help="Bucket"
            flag "--cache" help="Set cache directory"
            flag "--color" help="Colorize output"
            flag "--config" help="Set config file"
            flag "--config-format" help="Set config file format"
            flag "--debug" help="Include debug information in output"
            flag "--destination" help="Set destination directory"
            flag "--dry-run" help="Do not make any modifications to the destination directory"
            flag "--force" help="Make all changes without prompting"
            flag "--help" help="help for set"
            flag "--interactive" help="Prompt for all changes"
            flag "--keep-going" help="Keep going as far as possible after an error"
            flag "--key" help="Key"
            flag "--less-interactive" help="Prompt for changed or pre-existing targets"
            flag "--mode" help="Mode"
            flag "--no-pager" help="Do not use the pager"
            flag "--no-tty" help="Do not attempt to get a TTY for prompts"
            flag "--output" help="Write output to path instead of stdout"
            flag "--override-data" help="Override data"
            flag "--override-data-file" help="Override data with file"
            flag "--persistent-state" help="Set persistent state file"
            flag "--progress" help="Display progress bars"
            flag "--refresh-externals" help="Refresh external cache"
            flag "--source" help="Set source directory"
            flag "--source-path" help="Specify targets by source path"
            flag "--use-builtin-age" help="Use builtin age"
            flag "--use-builtin-diff" help="Use builtin diff"
            flag "--use-builtin-git" help="Use builtin git"
            flag "--value" help="Value"
            flag "--verbose" help="Make output more verbose"
            flag "--working-tree" help="Set working tree directory"
        }
    }

    cmd "status" help="Show the status of targets" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for status"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-P" help="Show status of all parent directories"
        flag "-p" help="Path style"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for status"
        flag "--include" help="Include entry types"
        flag "--init" help="Recreate config file from template"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--parent-dirs" help="Show status of all parent directories"
        flag "--path-style" help="Path style"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "target-path" help="Print the target path of a source path" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for target-path"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for target-path"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "unmanaged" help="List the unmanaged files in the destination directory" {
        flag "-0" help="Use the NUL character as a path separator"
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for unmanaged"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-p" help="Path style"
        flag "-R" help="Refresh external cache"
        flag "-S" help="Set source directory"
        flag "-t" help="Print paths as a tree"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for unmanaged"
        flag "--include" help="Include entry types"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--nul-path-separator" help="Use the NUL character as a path separator"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--path-style" help="Path style"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--tree" help="Print paths as a tree"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "update" help="Pull and apply any changes" {
        flag "-a" help="Apply after pulling"
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for update"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-P" help="Update all parent directories"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--apply" help="Apply after pulling"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for update"
        flag "--include" help="Include entry types"
        flag "--init" help="Recreate config file from template"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--parent-dirs" help="Update all parent directories"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recurse-submodules" help="Recursively update submodules"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }

    cmd "verify" help="Exit with success if the destination state matches the target state, fail otherwise" {
        flag "-c" help="Set config file"
        flag "-D" help="Set destination directory"
        flag "-h" help="help for verify"
        flag "-i" help="Include entry types"
        flag "-k" help="Keep going as far as possible after an error"
        flag "-n" help="Do not make any modifications to the destination directory"
        flag "-o" help="Write output to path instead of stdout"
        flag "-P" help="Verify all parent directories"
        flag "-R" help="Refresh external cache"
        flag "-r" help="Recurse into subdirectories"
        flag "-S" help="Set source directory"
        flag "-v" help="Make output more verbose"
        flag "-W" help="Set working tree directory"
        flag "-x" help="Exclude entry types"
        flag "--age-recipient" help="Override age recipient"
        flag "--age-recipient-file" help="Override age recipient"
        flag "--cache" help="Set cache directory"
        flag "--color" help="Colorize output"
        flag "--config" help="Set config file"
        flag "--config-format" help="Set config file format"
        flag "--debug" help="Include debug information in output"
        flag "--destination" help="Set destination directory"
        flag "--dry-run" help="Do not make any modifications to the destination directory"
        flag "--exclude" help="Exclude entry types"
        flag "--force" help="Make all changes without prompting"
        flag "--help" help="help for verify"
        flag "--include" help="Include entry types"
        flag "--init" help="Recreate config file from template"
        flag "--interactive" help="Prompt for all changes"
        flag "--keep-going" help="Keep going as far as possible after an error"
        flag "--less-interactive" help="Prompt for changed or pre-existing targets"
        flag "--mode" help="Mode"
        flag "--no-pager" help="Do not use the pager"
        flag "--no-tty" help="Do not attempt to get a TTY for prompts"
        flag "--output" help="Write output to path instead of stdout"
        flag "--override-data" help="Override data"
        flag "--override-data-file" help="Override data with file"
        flag "--parent-dirs" help="Verify all parent directories"
        flag "--persistent-state" help="Set persistent state file"
        flag "--progress" help="Display progress bars"
        flag "--recursive" help="Recurse into subdirectories"
        flag "--refresh-externals" help="Refresh external cache"
        flag "--source" help="Set source directory"
        flag "--source-path" help="Specify targets by source path"
        flag "--use-builtin-age" help="Use builtin age"
        flag "--use-builtin-diff" help="Use builtin diff"
        flag "--use-builtin-git" help="Use builtin git"
        flag "--verbose" help="Make output more verbose"
        flag "--working-tree" help="Set working tree directory"
    }